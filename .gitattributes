# The rendered documents are no longer tracked (see .gitignore); the rule is
# kept so that a copy added back by hand is still marked as generated rather
# than counted as the language composition of the compendium.
paper/barg/barg_report.html linguist-generated=true
